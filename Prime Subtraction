#include <bits/stdc++.h>
#define Tokisaki ios::sync_with_stdio(false); 
#define Kurumi cin.tie(0); cout.tie(0);
#define ll long long
using namespace std;

void solve(){
    ll x, y; cin >> x >> y;
    cout << ((x-y==1) ? "NO\n" : "YES\n");
}

int main(int argc, char const *argv[]){
    Tokisaki Kurumi
    int v_v; cin >> v_v; 
    while(v_v--) solve();
    return 0;
}
